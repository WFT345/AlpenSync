<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform theme parent: no appcompat/material-components dependency at M0.
         The Compose UI applies its own Material 3 theme on top. -->
    <style name="Theme.AlpenSync" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
    </style>
</resources>
